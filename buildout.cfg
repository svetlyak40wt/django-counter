[buildout]
parts = django
develop = .

[django]
recipe = djangorecipe
version = trunk
project = django_app_template
projectegg = ${django:project}
settings = testsettings
test = ${django:project}
eggs = ${django:project}

[buildout]
parts = django
develop = .

[django]
recipe = djangorecipe
version = 1.0.2
project = django_app_template
projectegg = ${django:project}
settings = testsettings
test = ${django:project}
eggs = ${django:project}
